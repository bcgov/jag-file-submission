import { Meta, Story } from "@storybook/addon-docs/blocks";
import { Alert } from "./Alert";
import * as stories from "./Alert.stories.js";

<Meta title="Alert" component={Alert} />

# Alert

Alert banners notify users of important information or changes on a page.
Typically, they appear at the top of a page.

## When to Use

Telling users important and typically time sensitive information. This includes:

- Success messages
- Error messages
- Warnings
- System statuses
- Important pieces of information

## When not to Use

- In-line error validation on form inputs
- Popular content on your website
- Highlighting content

## API

| Prop    | Type   | Required | Description                                                         | Default |
| ------- | ------ | -------- | ------------------------------------------------------------------- | ------- |
| type    | string | true     | indicates whether the alert is a warning, error, or success message |         |
| element | string | true     | message to be displayed within the alert                            |         |

<br />

## Success Alert

Used for success messages, typically for after an action is successfully performed.

<Story name="Success">{stories.Success()}</Story>

```bash
<Alert type="success" element="This is a success message!" />
```

## Warning Alert

Used for warning messages, typically for explicitly informing a user of something important that must be considered.

<Story name="Warning">{stories.Warning()}</Story>

```bash
<Alert type="warning" element="This is a warning message!" />
```

## Info Alert

Used for informative messages.

<Story name="Info">{stories.Info()}</Story>

```bash
<Alert type="info" element="This is an info message!" />
```

## Error Alert

Used for error messages, when something has gone wrong unexpectedly and the user needs to be aware of it.

<Story name="Error">{stories.Error()}</Story>

```bash
<Alert type="error" element="This is an error message!" />
```
