apiVersion: v1
kind: Service
metadata:
  annotations:
    openshift.io/generated-by: OpenShiftWebConsole
  labels:
    app: fla-nginx-proxy
  name: fla-nginx-proxy
  namespace: qzaydf-dev
spec:
  ports:
    - name: 8080-tcp
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    deploymentconfig: fla-nginx-proxy
  sessionAffinity: None
  type: ClusterIP
status:
  loadBalancer: {}
